# Catálogo — Sexo (`ddlAPP_GENDER`)

**Campo:** `ctl00_SiteContentPlaceHolder_FormView1_ddlAPP_GENDER` — pantalla
`Personal1` ("Sex"). Mapeado en `mapeo_ds160_1.json` a la variable `sexo`.

3 opciones (incluye el placeholder). No dispara postback. Formato:
`value|texto en inglés (DS-160)|traducción al español`.

```
|- Select One -|(sin seleccionar)
M|MALE|Masculino
F|FEMALE|Femenino
```
